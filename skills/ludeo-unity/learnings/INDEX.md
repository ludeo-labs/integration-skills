# Learnings Index (generated — do not edit by hand)

One line per learning: `path | tier | phase | hook`. The hook is the precondition
question when the learning has one, else its title. This index is a **pointer, not
the lesson** — never cite or apply a learning from its index line alone; read the body.

Regenerate with `node scripts/generate-learnings-index.mjs` after adding a learning.
If you add a learning where you cannot run node (e.g. an installed skill copy),
append the line by hand in the same format.

Total: 147

- architecture/a-replay-teleport-breaks-ring-ahead-level-streaming.md | generalizable | p5 | Does the game prepare its level content just-in-time around the player - room setup one ring ahead, encounters configured as neighbors unlock? A replay that teleports the player breaks the streaming's ordering assumptio…
- architecture/a-world-ready-flag-may-still-answer-for-the-world-you-are-leaving.md | generalizable | p5 | Does your restore flow poll a game-owned 'world/level is ready' flag to know when to restore? Find out what CLEARS it. If it is cleared by the loader or generator rather than by the load REQUEST, it still answers for th…
- architecture/activate-late-or-the-cloud-selects-before-the-game-can-load.md | generalizable | p3,7 | Does the integration call session.Activate() from an early hook (RuntimeInitializeOnLoadMethod, a bootstrap Awake) rather than when the game is genuinely ready to load a Ludeo? On a cloud machine the platform pushes Lud…
- architecture/an-empty-bucket-must-not-mean-everything-died.md | generalizable | p5 | Does your restore treat 'absent from the bucket' as 'this entity was destroyed before the capture instant'? Then decide NOW what it does when the bucket is empty and the scene is not — without a rail, one bad capture em…
- architecture/bind-openroom-to-sceneloaded-plus-manager-presence-not-a-named-entry.md | generalizable | p3 | In a level-based Unity game, are you binding the creator OpenRoom to a game entry method (a level-select handler, a level manager's Start) or to a build-index list - and have you accounted for the developer's Editor dir…
- architecture/boot-the-replay-through-the-games-own-entry-flow.md | generalizable | p5 | Does the game initialize the player through an entry flow (hub/lobby scene, spawn handshake, countdown) before gameplay levels? If the replay boot loads the level any other way, every skipped step of that flow is a late…
- architecture/capture-the-state-machine-not-its-derived-booleans.md | generalizable | p5 | Are you capturing an encounter/wave/phase system through boolean flags (isStarted, isFinished, isActive) instead of its underlying state enum? Find every writer of each boolean first - if any writer clears it while the …
- architecture/check-what-actually-ships-before-scoping-per-entity-work.md | generalizable | p4 | Before scoping per-entity work (bosses, level types, mechanics), open Build Settings and list which of those scenes are actually ENABLED. The shippable set is often a fraction of what the repo contains, and it decides w…
- architecture/classify-non-ludeoable-by-whether-the-sim-actually-freezes.md | generalizable | p3 | Are you deciding, per non-interactive segment, whether to emit StartNoneLudeable/StopNoneLudeable or PauseLudeo/ResumeLudeo?
- architecture/controls-gate-interface-is-the-non-ludeoable-census-and-a-zero-edit-suppression-seam.md | generalizable | p2 | Does the game gate player input through a one-property interface that a central controller collects with FindObjectsOfType and ANDs together — instead of through a central game-state enum?
- architecture/find-the-studio-s-own-repro-tooling-first.md | generalizable | p2 | Does this game generate its world at runtime (procedural / seeded), or otherwise have a QA 'reproduce this exact run' need? If so, search for the studio's own snapshot/repro dev tool BEFORE designing capture or restore.
- architecture/game-own-save-key-builder-is-a-ready-made-stable-key.md | generalizable | p4 | Does the game persist any per-object progression flags (permanent deaths, used/consumed elements, triggered events, opened chests) — and if so, how does it build the string key it stores them under?
- architecture/gate-playback-behavior-on-the-flow-switch.md | generalizable | p5,6 | Are you adding behavior that must run only during playback of a moment (or only during capture)? Gate it on the flow switch's explicit play-vs-create state, never on inferred leftovers.
- architecture/in-a-lives-based-game-the-run-ends-at-game-over-not-at-death.md | generalizable | p3 | Does the game have LIVES and a separate game-over screen (platformers, arcade shooters, roguelites with retries)? Then ask the integrator where the run ends — a death that reloads the level is usually the SAME run conti…
- architecture/jumping-straight-into-a-level-skips-setup-the-restore-needs.md | generalizable | p3,5 | Are you planning to boot the replay directly into the gameplay level (set a target scene, start the session, load) rather than going through the game's normal front-end flow? Test that shortcut early — it probably skips…
- architecture/level-reset-registry-is-census-batch-iterator-and-reset-seam.md | generalizable | p4 | Does the game have a level-restart / reset contract — a small interface with a per-level registry that every stateful object self-registers into from its own Start()?
- architecture/make-the-restore-verify-every-value-it-writes.md | universal | p5 | Make the restore verify every value it writes, and report it itself
- architecture/non-ludeoable-spans-as-a-state-machine-not-paired-calls.md | generalizable | p3 | Does the game have a central game-state enum plus a state-change notification that most of its non-interactive segments (menus, shops, dialogue, cutscenes, upgrade screens, map) announce themselves through?
- architecture/persisted-progression-can-be-a-generation-input.md | generalizable | p2 | Procedural world, and you're planning to reproduce it by capturing the RNG seed/state? Read the generator's own entry point for reads of PERSISTED player progression - many builders drop or unlock content based on save …
- architecture/put-the-session-teardown-in-the-scene-loader.md | generalizable | p3 | Is your run teardown hooked to the game's 'before I load the next scene' method? Grep for direct calls to the scene LOADER — every caller that skips that method is an exit path with no teardown, and a DontDestroyOnLoad …
- architecture/qa-needs-a-launcherless-auth-override-in-the-ship-build.md | generalizable | p7 | Will QA (or anyone) run the shipping build locally to CREATE Ludeos — i.e. outside the Ludeo launcher/platform? A ship-posture build (runWithoutLauncher=false) is Ludeo-inert when double-clicked.
- architecture/readiness-gate-cover-gate-the-grant-of-control-not-the-fade.md | generalizable | p3 | Are you choosing the SDK-readiness gate's 'ready cover', and is the game's existing fade/loading primitive driven by a fixed-length animation clip?
- architecture/replay-goal-needs-every-death-credited.md | generalizable | p6,8 | Can anything other than the player kill enemies - environment hazards, allies summoned by destructibles, kill planes, damage ticks - while the game's kill event fires only on player-attributed deaths?
- architecture/rest-in-flight-action-state-do-not-restore-it.md | generalizable | p5 | Does the character controller gate input or attacks on a state field that is cleared only at the END of a move, dodge, stun or knockback - a coroutine or frame-driven sequence the restore does not run? Restore that fiel…
- architecture/restored-entities-must-arrive-already-arrived.md | generalizable | p5 | Does the game reveal a newly spawned entity at the END of a timed arrival sequence - a dissolve, a fade-in, a spawn animation, an emerge-from-the-ground - rather than at the moment it is created? If so, a restore that s…
- architecture/room-ready-is-the-players-play-click.md | universal | p3,5 | In the player flow, RoomReady IS the player's "Play Moment" click — the whole flow hangs off that
- architecture/seed-replay-can-remove-the-placement-capture-entirely.md | generalizable | p4 | Procedural world, and you're about to plan capturing each room's resolved world transform? First check whether the room's transform is DERIVED from the content selection (a fixed coordinate→world mapping) or ROLLED inde…
- architecture/seed-replay-only-reproduces-what-the-seeded-stream-draws.md | generalizable | p1,4 | Procedural world? Map every random source in phase 1 KYG and record WHICH STREAM each load-bearing one draws from, then confirm empirically in phase 4 by replaying ONE clip twice and diffing the world. Generators routin…
- architecture/settle-the-rebuilt-moment-before-the-wait.md | universal | p5 | Settle the rebuilt moment: apply frozen, run half a second with input off, freeze again
- architecture/several-rooms-live-does-not-mean-the-world-streams.md | generalizable | p4 | Is the game procedural with several rooms/areas live and navigable at once? Before importing the open-world/streaming tracking delta, check separately whether the world actually streams OUT — 'multi-room' and 'streaming…
- architecture/span-machine-off-the-controls-gate-aggregate-when-there-is-no-state-enum.md | generalizable | p3 | Is the game's only agency lever a controls-gate interface rather than a central GameState enum - and are you about to place StartNoneLudeable/StopNoneLudeable at each non-interactive segment's enter and exit sites?
- architecture/split-the-seeded-stream-world-vs-combat.md | generalizable | p4 | Converting a game to seeded randomness for replay? Check WHAT ELSE draws from that stream. If any draw fires at player frequency - enemy AI decisions, drop rolls, chest contents - a single shared stream cannot reproduce…
- architecture/stamp-your-trace-with-the-sdks-own-clock-format.md | universal | p3,5 | Stamp every integration log line with the SDK's own clock format
- architecture/store-platform-removal-is-layered-not-a-single-fix.md | generalizable | p1,3,7 | Does the game require a store platform client (Steam / Epic / a console SDK) to reach gameplay? A Ludeo cloud machine has none of them — scope this as a layered removal, not a one-line fallback.
- architecture/the-replay-world-comes-pre-populated-reconcile-dont-add.md | generalizable | p5 | Does the replay rebuild the world through the game's normal level-build path, and does that path populate it (pre-spawned waves, default enemies, loot) on its own?
- architecture/the-restore-point-is-the-clip-start-not-the-capture-click.md | universal | p5 | The restore point is the clip START, not the capture click
- architecture/the-state-upload-is-lossy-above-a-ceiling.md | generalizable | p4,5 | How many objects does your capture track in the busiest level? Above roughly a hundred the state upload starts DROPPING objects silently — every CreateObject still returns Success — so scope the sweep on a logged per-ty…
- architecture/validate-placement-after-restore.md | generalizable | p5 | Has the restore's output ever been verified by the LAYER itself, rather than by a human watching the replay? If not, add placement validation before debugging anything else about replay fidelity — it converts 'the momen…
- architecture/wait-on-work-in-flight-not-on-a-flag-the-restore-suppressed.md | generalizable | p5 | Is the restore waiting on a game flag before it applies - a 'still loading' / 'still spawning' signal? Find out what LOWERS that flag. If the only thing that lowers it is the very system the restore suppresses, the wait…
- architecture/write-restored-state-to-whoever-owns-it.md | universal | p5 | Write restored state to whoever owns it, not to the field you can reach
- common-mistakes/a-boss-does-not-use-the-base-enemy-death-or-health-fields.md | generalizable | p5,6 | Does a boss inherit the base enemy class? Then check whether it actually uses the base class's death method and health field — bosses commonly count their own lives and route their defeat through their own method, so a …
- common-mistakes/a-code-map-line-reference-goes-stale-the-moment-you-edit-the-file.md | generalizable | p4,5,6 | About to follow a file:line reference recorded in an EARLIER phase's artifact (CODE_MAP, integration points, a TDD)? Check whether a later phase edited that file. Phase 3 inserts hooks into exactly the game files phase …
- common-mistakes/a-consumable-can-be-a-count-on-a-permanent-object-not-an-inventory-entry.md | generalizable | p4 | Does the game implement any pickup (potions, charges, consumables) as a count incremented on an object the player permanently owns, rather than as an inventory entry or a world object that transfers ownership?
- common-mistakes/a-deferred-game-command-stomps-freshly-restored-state.md | generalizable | p5 | Does your restore trigger any game flow through a networked command or async request (equip a weapon, change class, apply appearance)? Read what that flow does when it RESOLVES - if it resets or re-derives state you als…
- common-mistakes/a-discarded-result-code-turns-a-rejected-call-into-silence.md | universal | p6 | Are you logging 'action sent' from your own wrapper, using an SDK overload that returns void or discards its LudeoResult? Then your log says 'we called it', never 'the SDK accepted it' — switch to the overload that retu…
- common-mistakes/a-finished-moment-can-still-be-one-frozen-frame.md | generalizable | p5,7 | Is a captured moment unplayable, or does it show the wrong thing, while every signal you can reach says the capture succeeded? Before explaining WHY the wrong content was captured, open the video and confirm the recorde…
- common-mistakes/a-generated-event-enum-is-not-an-event-bus.md | generalizable | p6 | Does the game have a data-driven progression / quest / achievement system with a generated enum of typed gameplay events (OnEnemyKill, OnPlayerDeath, OnLevelClear...) and a single Trigger/Raise/Dispatch method? Before p…
- common-mistakes/a-global-replay-gate-on-wave-triggers-starves-untriggered-encounters.md | generalizable | p5 | Does the game populate rooms/encounters through a start trigger (first wave, proximity, door crossing) that must still fire for fights the player reaches during a replay's forward play?
- common-mistakes/a-green-compile-does-not-prove-your-edit-compiled.md | generalizable | p3,5,6 | Are you adding a member to an existing GAME file (an accessor so the layer can read private state, a hook call)? Before trusting the compile gate that follows, confirm the insertion point is LIVE code — an addition noth…
- common-mistakes/a-guard-that-cannot-fire-is-not-evidence.md | generalizable | p3,5 | Are you citing the ABSENCE of an error line as proof a code path got past a guard? Check the guard can actually fire at all — a singleton accessor that lazily CREATES its instance never returns null, so the null check b…
- common-mistakes/a-latch-cleared-by-an-animation-event-looks-like-dead-code-to-a-cs-grep.md | generalizable | p5 | Are you deciding whether a captured 'was hit / staggered / interrupted' latch can be restored verbatim — and did you grep only .cs files for the method that clears it?
- common-mistakes/a-loading-screen-open-call-may-silently-drop-your-callback.md | generalizable | p5 | Are you passing a callback into the game's own loading-screen/transition API (open/show/fade), with anything load-bearing inside it? Read that API's guard clauses first: many such methods silently return WITHOUT invokin…
- common-mistakes/a-mode-flag-that-early-returns-past-the-controls-arbiter-locks-the-gate-shut.md | generalizable | p3 | Are you relying on a controls-gate interface as your zero-edit suppression seam — and does the arbiter's Update have any EARLY RETURN above the line that re-evaluates the gates?
- common-mistakes/a-path-that-skips-the-menus-skips-what-the-menus-initialise.md | generalizable | p3,5 | Does your restore or boot-straight path reach gameplay without going through the game's front end? Enumerate what the menus initialise — HUD, input-device binding, per-player widget lists, difficulty/mode state — becaus…
- common-mistakes/a-player-only-instrument-hidden-behind-unity-editor-is-never-compiled.md | generalizable | p7 | Are you adding player-only code (a cloud log mirror, a P/Invoke, a platform shim) behind `#if ... && !UNITY_EDITOR`, in a project whose compile gate is a headless Editor run? That gate cannot see inside the block — it w…
- common-mistakes/a-pooled-entity-needs-the-games-activation-not-just-its-state-flags.md | generalizable | p5 | Do the game's pooled entities go through an explicit activation step after leaving the pool (StartAI, a state-machine kick, a wave trigger) - i.e. is a freshly-pulled instance 'parked' until something starts it?
- common-mistakes/a-progression-flag-force-needs-a-record-once-undo-ledger.md | generalizable | p5 | Are you forcing a PERSISTED progression flag (PlayerPrefs / a save key) before the restore's scene load, to suppress an intro or tutorial that HOLDS PLAYER CONTROL — and can that pre-load hook be re-entered by replay-to…
- common-mistakes/a-restore-write-another-component-re-asserts-every-frame-survives-one-frame.md | generalizable | p5 | Did the restore write a GameObject's active state, a component's enabled flag, or a mode flag — and is there any OTHER component that re-asserts that same value from its own Update/LateUpdate every frame?
- common-mistakes/a-restore-write-can-be-silently-vetoed-by-gameplay-rules.md | generalizable | p5 | Are you restoring a stat (health, resource, timer) by assigning through the same setter gameplay uses? Check that setter for interceptor hooks - damage gates, invulnerability windows, clamps, die-state guards. Any of th…
- common-mistakes/a-round-trip-can-be-correct-only-in-the-default-case.md | generalizable | p5 | Does the getter you capture return a value the game has ALREADY combined (magnitude times sign, local plus offset, base times multiplier), while the setter you restore through re-applies the same factor? The round trip …
- common-mistakes/a-scenes-yaml-does-not-list-what-its-prefabs-contain.md | generalizable | p4 | Are you about to state what a scene contains (or does not contain) from parsing the .unity file's script references? Prefab-instance children are NOT in that file — cross-check against the restore's matched counts or an…
- common-mistakes/a-session-boundary-can-exist-and-never-fire.md | generalizable | p2 | Are you recording a gameplay start/end boundary because you found a method, an event, and a subscriber for it? Check that something actually CALLS the firer - a fully wired boundary can still be dead code, and the wirin…
- common-mistakes/a-stable-key-must-travel-not-be-recomputed.md | generalizable | p4,5 | About to rule out a candidate stable key because its value was decided at runtime — a rolled spawn position, a random pick, anything non-deterministic? That is the wrong test. A key has to be unique and unchanging WITHI…
- common-mistakes/a-transient-state-classification-expires-when-a-later-wave-restores-its-driver.md | generalizable | p5 | Did an earlier wave put a state on the transient/normalize list *because nothing in the restore could advance the entity out of it* — and does the wave you are implementing now restore the thing that advances it?
- common-mistakes/agent-can-run-unity-compile-gates-headlessly.md | generalizable | p1,3,5,6 | Is a matching Unity Editor installed on this machine, with shell access, and the project NOT currently open in the Editor (no Temp/UnityLockfile)? (Applies to EVERY compile gate, not just phase 1.)
- common-mistakes/an-optional-notify-flag-makes-the-pause-event-lie.md | generalizable | p3 | About to report pause/resume by subscribing to the game's pause EVENT? Read the pause method's signature first - if it takes a 'show the menu' / 'notify' style bool, some callers pass false and freeze the game without e…
- common-mistakes/ask-what-your-check-cannot-see.md | universal | p1,2,3,4,5,6,7,8 | Ask what your check *cannot* see, not just what it returned
- common-mistakes/async-abort-outruns-the-next-runs-openroom-intent.md | generalizable | p3 | Does the game's restart path Abort the old run and start the new one in the SAME synchronous call chain — i.e. does the next run's OpenRoom trigger fire before the Abort/CloseRoom callbacks have completed?
- common-mistakes/audit-the-end-boundary-as-hard-as-the-start.md | generalizable | p3 | Binding the END of the capture window to a game method (return-to-hub, back-to-lobby, quit-to-menu)? Enumerate that method's callers BY ENCLOSING METHOD, not by count. A 'return to X' method is very often also the BOOT …
- common-mistakes/check-the-pause-primitive-for-terminal-callers.md | generalizable | p6 | About to emit the Pause/Resume span pair from the game's pause primitive? Read EVERY call site first — a symmetric-looking pause function is commonly also used as a terminal freeze (death, level end) that nothing ever r…
- common-mistakes/check-the-restore-cannot-fire-your-kill-action.md | generalizable | p6 | Before mapping a Kill (or any 'entity destroyed') action in phase 6: does the phase-5 restore put recorded-dead entities back through the game's own death method? If it does, every replay fires one Kill per corpse at bo…
- common-mistakes/date-the-served-snapshot-before-blaming-the-capture.md | generalizable | p5,7 | Report that the replay starts 'slightly behind' where the moment shows? Before touching the capture code, DATE the served snapshot against the level's authored start values — and check whether anyone ever called the roo…
- common-mistakes/dotnet-build-of-the-unity-csproj-cannot-catch-player-build-breaks.md | generalizable | p7 | Are you using `dotnet build` on Unity's generated csproj as your compile check — and has this project ever actually produced a player build?
- common-mistakes/dramatized-kills-open-a-non-ludeoable-span-before-the-action-fires.md | generalizable | p6 | Does the game dramatize its most important beats (boss kill, player death, finisher) with a cinematic state change — and does that state write happen BEFORE the kill/death call the action would be emitted from?
- common-mistakes/ending-a-recording-is-async-do-not-shut-down-on-the-next-line.md | universal | p3 | Ending a gameplay session is a network round-trip — disposing on the next line cancels it
- common-mistakes/enumerate-captures-from-the-sdk-log-not-from-what-you-were-handed.md | generalizable | p5,7 | Are you reasoning about which moments a session captured, from a platform UI list or from a folder of files someone collected for you? Enumerate them from the SDK's own onCaptureVideoRequest lines first — the handed-ove…
- common-mistakes/field-dispositions-approve-fields-but-capture-needs-accessors.md | generalizable | p5 | Did the per-entity field-disposition table (phase 4 Part B / phase 5 task 0) check READABILITY of each capture-marked field from outside its class, or only that the field exists?
- common-mistakes/forcing-a-setting-for-a-replay-must-not-persist-it.md | generalizable | p3,5 | Are you forcing a game setting for a replay or a boot path (player count, language, screen mode, difficulty) by calling the game's own setter? Check whether that setter WRITES THE SAVE — if it does, one replay silently …
- common-mistakes/headless-smoke-test-instead-of-editor-init.md | generalizable | p1 | Can the agent launch Unity itself (Editor installed locally, project not locked), so the phase-1 Initialize() smoke test can run headlessly instead of as a hand-clicked Editor action?
- common-mistakes/hook-the-scene-load-funnel-below-its-already-loading-guard.md | generalizable | p3 | Are you putting the CR-007 End/Abort hook in a wrapper around SceneManager.LoadSceneAsync — and does that wrapper early-return when a load is already in flight?
- common-mistakes/inactive-until-triggered-entities-are-a-third-restore-case.md | generalizable | p5 | Is your restore's reconciliation a two-way choice between MATCH (a live scene instance) and SPAWN (a clone) — and does the 'live scene instance' side enumerate the game's own runtime registry rather than the scene?
- common-mistakes/interrupt-unsafe-ability-toggles-strand-enemies.md | generalizable | p5,8 | Do any enemy abilities toggle collision or visibility OFF at a start stage and back ON only at a later stage (a leap that ghosts through obstacles, a burrow, a teleport fade)? Replayed fights maximize mid-window interru…
- common-mistakes/investigate-before-asking.md | universal | p1,2,3,4,5,6,7,8 | Do the work before you speak — never ask what the repo answers, never assert what you haven't checked
- common-mistakes/match-a-stable-key-by-proximity-not-equality.md | generalizable | p5 | Is your entity key derived from a float coordinate - a spawn point, an authored position - and matched on restore by equality or by a quantised hash? Capture and replay build their worlds independently, so those floats …
- common-mistakes/moment-is-the-integrators-word-do-not-redefine-it.md | universal | p1,2,3,4,5,6,7,8 | "Moment" is the integrator's word for the thing they mark — never reuse it for the session bracket
- common-mistakes/never-call-into-the-sdk-from-inside-an-sdk-callback.md | universal | p3,5 | Never issue an SDK call from inside an SDK callback
- common-mistakes/normalize-the-whole-agency-suppressing-flag-family-not-just-the-input-one.md | generalizable | p5 | Does the game suppress player agency during scripted sequences with MORE THAN ONE flag (an input-disable, a game-state enum, a movement-only/no-combat mode) — and does the restore normalize ALL of them, or only the obvi…
- common-mistakes/objecttype-is-the-objects-identity-not-its-class-name.md | universal | p5 | objectType is the object's identity, not its class name
- common-mistakes/one-base-class-register-hook-misses-subclasses-that-skip-base-start.md | generalizable | p5 | Are you putting the collection's register hook in a BASE MonoBehaviour's Start()/Awake() so one site covers every subclass — and have you verified that every subclass actually calls base.Start()?
- common-mistakes/one-throwing-writer-silently-drops-every-object-after-it-in-the-sample-loop.md | generalizable | p5 | Does your OnStateDataUpdate writer read a MonoBehaviour/Transform/GameObject it captured at registration — and is the per-tick sampler a plain for-loop over every tracked handler with no per-handler isolation?
- common-mistakes/owner-attribution-is-not-enough-restore-needs-the-owners-specific-collection.md | generalizable | p5 | Are you capturing an OWNER reference so the restore can re-register spawned entities with the thing that produced them — and does that owner hold MORE THAN ONE collection of spawned entities?
- common-mistakes/pair-a-teleport-with-the-games-state-updates.md | generalizable | p5 | Before calling a game's low-level teleport/placement method from the restore, read the game's OWN call sites of that method: do they pair it with sibling state updates (room membership, spatial index registration, camer…
- common-mistakes/per-frame-open-needs-a-closed-latch.md | generalizable | p3,5 | Is the capture segment opened from a per-frame game tick (rather than a one-shot event)? If so, does anything stop the very next tick from reopening it after a deliberate close?
- common-mistakes/pool-hooks-alone-miss-entities-that-predate-the-capture.md | generalizable | p5 | Does the game create any tracked entities (pre-spawned waves, pre-placed enemies, warm pools) BEFORE the capture segment opens - e.g. during level build, which finishes before gameplay starts?
- common-mistakes/prefer-a-signal-over-polling-in-per-tick-capture.md | universal | p5 | Never discover state by scanning from a per-tick capture writer — take the signal
- common-mistakes/progression-flags-delete-the-captured-object-before-restore-can-match-it.md | generalizable | p5 | Does the game destroy or skip-spawn any entity at scene load based on a PERSISTED progression flag (permanent death, chest opened, event fired, one-shot consumed) — and does the restore reload that scene against the VIE…
- common-mistakes/prove-a-genre-checklist-type-is-live-in-the-shipping-config-first.md | generalizable | p4 | Is a census type on your list because the genre checklist demands it and a matching class/counter exists in the codebase — rather than because you proved the mechanic is live in the SHIPPING prefabs and scenes?
- common-mistakes/prove-the-log-came-from-the-build-you-just-made.md | generalizable | p1,3,5,6,7 | Are you about to read a runtime log to judge a change you just built? A player build takes minutes and rewrites the previous player in place, so a run launched during that window exercises the OLD binary - and its log l…
- common-mistakes/raise-the-ready-cover-at-scene-activation-not-after-a-grace-period.md | generalizable | p3 | Are you showing the readiness-gate cover lazily - only once the control hold outlives a grace period - so that the healthy path stays invisible?
- common-mistakes/readiness-latch-and-per-run-control-hold-must-not-share-a-release-token.md | generalizable | p3 | Does your readiness gate use ONE release token for both 'SDK readiness resolved' (once per app run) and 'the player may have control now' (once per gameplay run) - and can readiness resolve AFTER a level has already sta…
- common-mistakes/renderer-enabled-is-not-visible-to-the-player.md | generalizable | p5,8 | Are you diagnosing entities the player reports as missing or invisible, with a check that reads renderer.enabled (or any component flag)? Component state is not what the player sees - measure the player's view.
- common-mistakes/replaying-from-inside-the-same-level-needs-a-hop-out.md | generalizable | p5 | What happens when the player replays a clip recorded in the level they are CURRENTLY standing in? If the game's scene-change path early-returns when the target scene is already active (most do), the replay boot silently…
- common-mistakes/restoring-an-already-seen-latch-does-not-suppress-what-it-gates.md | generalizable | p5 | Are you relying on a restored 'already seen / already done' latch to stop a cutscene or one-shot from re-firing — and inside the driver's Update, does the FIRE branch run before the branch that reads the latch?
- common-mistakes/roomready-is-the-press-play-signal-so-applying-there-hides-the-moment.md | generalizable | p5 | Did you pick the apply-inside-onRoomReady placement AND drop your loading cover in Begin's callback — i.e. is anything the viewer is supposed to SEE before pressing Play gated on RoomReady?
- common-mistakes/sweep-the-base-class-not-just-the-components.md | generalizable | p4,5 | About to enumerate an entity's field surface from the components on its GameObject? Check what those components INHERIT. The skill tells you to sweep sideways (other components, managers, ScriptableObjects) but not upwa…
- common-mistakes/the-batch-passes-own-reset-clears-the-pending-queue-it-must-drain.md | generalizable | p5 | Did you implement the 'remember, don't discard' pending queue for pre-live spawn notifications — and does your batch pass call a Reset()/Clear() on its own registry before it drains that queue?
- common-mistakes/the-existing-phase-entry-point-is-the-dramatizer-not-a-setter.md | generalizable | p5 | Are you planning to restore a boss phase / stage / form index by calling the game's existing AdvancePhase()-style entry point, on the grounds that a public entry point already exists?
- common-mistakes/the-per-class-register-hook-fires-before-capture-goes-live.md | generalizable | p5 | Are you adding a per-class register hook to a Start()/Awake() to reach an entity the base hook and the backstop sweep both miss — and does your registry DISCARD register notifications that arrive before the room is open?
- common-mistakes/the-restore-freeze-parks-the-rooms-own-setup-and-it-resumes-on-top-of-you.md | generalizable | p5 | Does the level/room run any of its own setup on a SCALED-time coroutine (WaitForSeconds, a spawn delay, a wave warm-up)? Your restore hold parks it mid-flight. It does not die there - it resumes the moment you unfreeze,…
- common-mistakes/the-window-between-scene-load-and-apply-is-live-because-timescale-zero-does-not-stop-update.md | generalizable | p5 | Does your restore hold Time.timeScale = 0 from scene load until the apply runs at RoomReady — and are you treating that window as inert?
- common-mistakes/timescale-arbiter-must-restore-the-observed-value-not-1f.md | generalizable | p3 | Before planning the CR-010 freeze / CR-011 overlay pause, did you grep every Time.timeScale write in the game — and did any of them write a value that is neither 0 nor 1?
- common-mistakes/verify-a-relayed-question-before-asking-it.md | generalizable | p3,5,6 | Are you running an orchestrated phase (3, 5 or 6) where a subagent has surfaced a question for the human?
- common-mistakes/verify-the-define-fence-before-citing-a-hook.md | generalizable | p2 | Does the target build use a define set that differs from the Editor's (a demo/shipping/console variant that turns dev tools OFF)? If so, every hook you record must be checked against the fence it sits inside.
- common-mistakes/verify-the-world-not-the-flag-you-just-wrote.md | universal | p5,6 | Never verify a restore against a field the restore itself writes
- common-mistakes/write-for-the-integrator-not-for-the-skill.md | universal | p1,2,3,4,5,6,7,8 | Write for the integrator, not for the skill — they never agreed to learn our vocabulary
- common-mistakes/your-replay-branch-bypasses-the-rest-of-the-entry-switch.md | generalizable | p5 | Did you add an early-return replay branch to the game's level-entry method? List every branch BELOW yours in that method — you have bypassed all of them, including the one that starts the level's antagonist.
- engine-quirks/a-game-clock-freeze-still-animates-on-the-engine-clock.md | generalizable | p5 | Does the game run its own clock (a GameTime with per-system scales, deltas built from unscaledDeltaTime)? Then freezing that clock stops LOGIC but not PRESENTATION - animators, particles, engine physics and tree ticks r…
- engine-quirks/a-looping-playonawake-video-crashes-the-cloud-build.md | generalizable | p7,8 | Does the game ship any VideoPlayer with m_PlayOnAwake: 1 — especially one nested in an always-loaded UI canvas prefab? On a Ludeo cloud machine that runs under Wine, whose Media Foundation implementation is far less for…
- engine-quirks/a-scene-load-stomps-your-freeze-re-assert-it.md | generalizable | p5 | Does your restore hold timeScale at 0 across a scene LOAD - a hop-out, a level swap, a menu round-trip? The incoming scene's Awake/Start is entitled to set timeScale, and a re-Hold() will not recover it if your freeze o…
- engine-quirks/bounded-waits-need-absolute-deadlines-not-accumulated-deltatime.md | generalizable | p3 | Is any readiness/Begin timeout implemented as a coroutine accumulating Time.unscaledDeltaTime, or armed at scene activation to cover both the Activate/consent wait AND the room chain?
- engine-quirks/buildplayer-unloads-assets-held-across-the-build.md | generalizable | p7 | Does a build script hold a loaded asset reference (ScriptableObject, prefab, settings asset) across a BuildPipeline.BuildPlayer call and write to it afterwards — e.g. to restore a temporarily flipped configuration?
- engine-quirks/buildscriptsonly-is-not-scripts-only-with-addressables.md | generalizable | p3,5,6,7 | Does the game use Addressables (or any package with an IPreprocessBuildWithReport/build-hook that produces content)? If so, BuildOptions.BuildScriptsOnly will NOT give you a fast, asset-safe rebuild.
- engine-quirks/cloud-proton-drops-unity-stdout-so-logfile-dash-buys-nothing.md | generalizable | p7 | Is the build going to the Ludeo cloud (Proton/umu runner)? Then check whether the game's own Unity log actually reaches the collected artifacts before relying on `-logFile -`.
- engine-quirks/deleting-scriptassemblies-does-not-force-a-recompile.md | generalizable | p1,3,5,6 | Are you establishing a compile baseline (or checking whether a change added warnings) by deleting Library/ScriptAssemblies and re-running -batchmode? Unity's Bee build cache can restore the assemblies without re-running…
- engine-quirks/dummyossystems-called-too-early-warning-is-benign.md | generalizable | p1 | Did Initialize() log a yellow 'DummyOSSystems SetLoggerCallbacks (called too early?)' warning?
- engine-quirks/editor-stop-play-does-tear-down-the-native-layer.md | generalizable | p1 | Are you deciding how to run the phase-1 Editor smoke test, or worried Initialize() will leave the Editor cursor hooked after stop-play?
- engine-quirks/hand-author-ludeosettings-asset-write-every-field.md | generalizable | p1 | Do you want to set LudeoSettings' apiKey / launcherUserId / betaVersion from the agent side rather than having the integrator type them into the Inspector?
- engine-quirks/headless-editor-setup-needs-executemethod.md | generalizable | p1 | Is the Ludeo package being installed or configured headlessly (-batchmode, CI, or an agent driving Unity), rather than by a human in an interactive Editor session?
- engine-quirks/headless-player-build-needs-a-scene-open.md | generalizable | p3,7 | Does the game implement BuildPlayerProcessor / IPreprocessBuildWithReport? If so, check whether it reads or restores the currently-open scene — that assumption breaks under -batchmode.
- engine-quirks/layer-namespace-can-silently-inherit-the-games-base-types.md | generalizable | p3 | Is the Ludeo layer being placed in a namespace nested under the game's root namespace? If so, grep that root for types shadowing common framework names (`MonoBehaviour`, `Environment`, `Object`, `Random`, `Time`, `Debug…
- engine-quirks/ludeosettings-namespace-is-ludeosdk-unityscripts-not-ludeosdk.md | generalizable | p7 | Are you writing the phase-7 build-gate hook (or any editor script) that does Resources.Load<LudeoSettings>, on plugin 4.3.1.0 or newer?
- engine-quirks/native-core-version-differs-from-plugin-version.md | generalizable | p1 | Does the native core version logged at Initialize() differ from the UPM package version you installed?
- engine-quirks/non-positive-physics-simulate-spams-negative-time.md | generalizable | p5 | Does the game step physics from script (SimulationMode.Script + Physics.Simulate(dt)) with a dt derived from a game-owned time scale? If the restore freeze zeroes that scale, every FixedUpdate calls Simulate with a non-…
- engine-quirks/one-shot-ui-reveals-lose-races-against-hud-rebuilds.md | generalizable | p5 | Does the HUD rebuild itself asynchronously on level load (clear-and-recreate of map tiles, markers, widgets), while the things it displays are revealed by ONE-SHOT calls (a door drawn on room entry, an enemy dot switche…
- engine-quirks/postprocess-auto-runs-setup-ludeo-sdk-on-import.md | generalizable | p1 | Are you treating 'Ludeo → Setup Ludeo SDK' as a required manual step, and did you see UpdateLudeoCoreDllReference logged twice?
- engine-quirks/prefab-composed-levels-hide-their-managers-from-scene-greps.md | generalizable | p2 | With ForceText serialization, did grepping a level .unity for a manager's script guid return zero — even though the game obviously has that manager at runtime?
- engine-quirks/prefab-instance-counts-overcount-a-behaviour-that-instances-remove.md | generalizable | p4 | Are you censusing how many instances of a BEHAVIOUR a level has by counting prefab instances of the prefab that hosts it — and have you checked m_RemovedComponents on each instance?
- engine-quirks/reading-the-live-editor-log-needs-shared-file-access.md | generalizable | p1,3,5,6 | Are you writing tooling (or any .NET/PowerShell code) that reads Editor.log while the Unity editor is still running? The default file-open path throws 'used by another process' — you must open it with FileShare.ReadWrit…
- engine-quirks/scene-script-guid-counts-overcount-via-stripped-prefab-components.md | generalizable | p4 | Are you counting a script's guid occurrences in a .unity file to census how many instances of a type a level has — and are you filtering out the entries whose m_GameObject is fileID 0?
- engine-quirks/schedule-clocks-tick-through-the-pre-play-hold.md | generalizable | p5 | Do wave/encounter schedulers run on a clock your pre-play hold does not freeze? Compare the scheduler's elapsed at Begin against the restored value - drift there silently trims the spawn schedule's tail.
- engine-quirks/sdk-setup-needs-two-headless-runs.md | generalizable | p1 | Is LudeoUnityEditorHelpers.SetupLudeoAssets being run headlessly for the FIRST time, i.e. before LudeoSettings.asset exists on disk?
- engine-quirks/the-cloud-crash-dump-is-inside-the-container.md | generalizable | p7,8 | Chasing a crash on a Ludeo cast machine and only pulling /home/ubuntu/game-logs/<session>/? Unity's crash dump is not there — it is inside the Docker container, in the Wine prefix, and the container is deleted when the …
- engine-quirks/the-first-setup-run-throw-is-version-dependent.md | generalizable | p1 | Running SetupLudeoAssets headlessly and judging whether it worked? Do not use 'did the first run throw?' as the signal - the throw is version-dependent and was absent in plugin 4.3.2. Grep for the last step's own log li…
- engine-quirks/the-overlay-takes-focus-so-runinbackground-must-be-on.md | generalizable | p1,7 | Check Player Settings for runInBackground, and check what fullscreen mode the game actually uses. With runInBackground off, the Ludeo overlay taking focus PAUSES the whole game mid-capture; with exclusive fullscreen, Wi…
- engine-quirks/timescale-zero-stops-the-sdk-notification-pump.md | universal | p5 | Does your restore freeze the game with Time.timeScale = 0 at any point BEFORE an SDK callback you are still waiting for (RoomReady, AddPlayer, BeginGameplay)? The plugin's notification pump stops at timeScale 0 and the …
- engine-quirks/which-project-files-you-may-edit-under-a-live-editor.md | generalizable | p4 | Are you about to edit a scene, prefab or ProjectSettings asset from outside Unity — and is the Editor open on that project right now?
- save-systems/serialized-id-keys-collapse-where-the-id-minter-never-ran.md | generalizable | p4 | Are you reusing the game's own per-object save id as the Ludeo stable key — and is that id a [SerializeField] on a PREFAB, minted by an editor utility that someone has to remember to run per scene?
